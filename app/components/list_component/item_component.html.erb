<dt class="<%= "#{@cls1} #{@i % 2 == 0 ? "bg-primary" : "bg-secondary"}" %>">
  <%= @key || key %>
</dt>
<dd class="<%= "#{@cls2} #{@i % 2 == 0 ? "bg-primary" : "bg-secondary"}" %>">
  <%= @value || value %>
</dd>
